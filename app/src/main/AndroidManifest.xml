<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      [[ OVERSEER PERMISSION MODULE ]]
      This <queries> block resolves the Package Visibility issue on Android 11+.
      It tells the OS: "I intend to interact with these specific packages."
    -->
    <queries>
        <package android:name="com.snakesan.cyberquake" />
        <package android:name="com.snakesan.vitalitysys" />
        <package android:name="com.example.besu" />
        <package android:name="com.snakesan.neonflux" />
    </queries>

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-feature android:name="android.hardware.type.watch" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Wear OS Libraries -->
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <uses-library
            android:name="wear-sdk"
            android:required="false" />
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />


        <!-- Standalone Mode: True -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!--
           Main Entry Point
           Matches your folder structure: com.snakesan.overseer.presentation
        -->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting"
            android:turnScreenOn="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>


    </application>

</manifest>